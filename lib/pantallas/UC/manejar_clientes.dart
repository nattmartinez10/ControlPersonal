import 'package:flutter/material.dart';

class ManageClientsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manage Clients'),
      ),
      body: Center(
        child: Text('Manage Clients Screen'),
      ),
    );
  }
}
