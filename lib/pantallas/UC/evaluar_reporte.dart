import 'package:flutter/material.dart';

class EvaluateReportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Evaluate Reports'),
      ),
      body: Center(
        child: Text('Evaluate Reports Screen'),
      ),
    );
  }
}
